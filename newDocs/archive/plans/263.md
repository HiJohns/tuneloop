# Issue #263: [Sub-task of #260] 预检 API

## 任务目标
实现导入前的预检功能。

## 具体要求
1. 端点: `POST /api/instruments/batch-import/preview`
2. 返回识别数量、警告、错误
3. 不执行实际导入

## 依赖
- 依赖 #262 (名称映射与图片处理)

## 实现
- 在 `backend/handlers/instrument_batch_import.go` 添加 PreviewBatchImport 函数
- 解析 ZIP 但不写入数据库
- 返回预检报告：识别的乐器数量、警告列表、错误列表

## 文件
- `backend/handlers/instrument_batch_import.go`

## 验收标准
- 运行 `go build` 确保代码可编译构建
- 运行相关单元测试

---
*Model: minimax-m2.5-free*