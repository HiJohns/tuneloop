# IAM Configuration for PC Frontend
VITE_BEACONIAM_EXTERNAL_URL=http://opencode.linxdeep.com:5552
# OAuth client_id is served by /api/config at runtime (from IAM_NAMESPACE)
# Build-time fallback only — remove when /api/config is always available
VITE_IAM_PC_REDIRECT_URI=http://localhost:5554/callback
