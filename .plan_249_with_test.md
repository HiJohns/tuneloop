# Issue #249 修复计划（更新）：GET /api/sites/tree 返回 manager 为 null

## 📌 问题确认

根据最新评论，manager_id 已保存到数据库，但 API 返回的 `manager` 字段仍为 null。

## 🔍 根因定位

### 可能原因 1: GORM 字段映射
Site 模型的 ManagerID 字段定义：
```go
ManagerID *uuid.UUID `gorm:"type:uuid;index" json:"manager_id"`
```

**问题**: GORM 默认使用 snake_case，应该映射到 `manager_id` 列。但可能数据库列名不匹配。

### 可能原因 2: 查询未加载 manager_id
在 GetSiteTree 中使用 `query.Find(&sites)`，应该加载所有列。

### 可能原因 3: lookupOrSyncManager 失败
即使 ManagerID 有值，lookupOrSyncManager 调用可能失败导致 manager 为 null。

## 📐 修复计划

### 修改 1: 显式指定 GORM 列名

**文件**: `backend/models/models.go`

```go
ManagerID *uuid.UUID `gorm:"column:manager_id;type:uuid;index" json:"manager_id"`
```

### 修改 2: 添加调试日志

**文件**: `backend/handlers/site.go` (GetSiteTree 函数)

在遍历 sites 时添加日志：
```go
for _, site := range sites {
    fmt.Printf("[DEBUG] Site ID: %s, ManagerID: %v\n", site.ID, site.ManagerID)
    // ... rest of code
}
```

### 修改 3: 检查 lookupOrSyncManager 错误处理

**文件**: `backend/handlers/site.go` (约第 526-539 行)

确保错误被正确记录：
```go
if site.ManagerID != nil {
    user, err := lookupOrSyncManager(c, db, site.ManagerID.String())
    if err != nil {
        fmt.Printf("[ERROR] Failed to lookup manager %s: %v\n", site.ManagerID.String(), err)
    } else if user != nil {
        // ... set manager
    }
}
```

### 修改 4: 添加单元测试覆盖

**文件**: `backend/handlers/site_test.go` (新建或修改)

创建基于 `httptest` 的单元测试：

```go
func TestGetSiteTreeReturnsManager(t *testing.T) {
    gin.SetMode(gin.TestMode)
    
    cfg := database.LoadConfig()
    db, err := database.InitDB(cfg)
    if err != nil {
        t.Skip("Test database not available, skipping:", err)
        return
    }
    database.SetDB(db)
    
    tenantID := uuid.New().String()
    now := time.Now()
    
    // 1. 创建测试管理员用户
    managerID := uuid.New()
    db.Exec(`INSERT INTO users (id, name, email, tenant_id, created_at) 
        VALUES (?, '测试管理员', 'test@example.com', ?, ?)`,
        managerID, tenantID, now)
    
    // 2. 创建测试网点（带 manager_id）
    siteID := uuid.New().String()
    db.Exec(`INSERT INTO sites (id, name, address, tenant_id, manager_id, created_at, updated_at) 
        VALUES (?, '测试网点', '测试地址', ?, ?, ?, ?)`,
        siteID, tenantID, managerID, now, now)
    
    // 3. 设置路由
    router := gin.New()
    router.Use(func(c *gin.Context) {
        ctx := context.WithValue(c.Request.Context(), middleware.ContextKeyTenantID, tenantID)
        c.Request = c.Request.WithContext(ctx)
        c.Next()
    })
    router.GET("/api/sites/tree", GetSiteTree)
    
    // 4. 调用 GET /api/sites/tree
    req := httptest.NewRequest("GET", "/api/sites/tree", nil)
    w := httptest.NewRecorder()
    router.ServeHTTP(w, req)
    
    // 5. 验证响应
    assert.Equal(t, http.StatusOK, w.Code)
    
    var response struct {
        Code int `json:"code"`
        Data struct {
            List []struct {
                ID       string `json:"id"`
                Name     string `json:"name"`
                Manager  *struct {
                    ID   string `json:"id"`
                    Name string `json:"name"`
                } `json:"manager"`
            } `json:"list"`
        } `json:"data"`
    }
    
    err = json.Unmarshal(w.Body.Bytes(), &response)
    require.NoError(t, err)
    
    // 6. 验证 manager 不为 null 且包含正确的 name
    require.NotEmpty(t, response.Data.List, "Should return sites list")
    foundSite := false
    for _, site := range response.Data.List {
        if site.ID == siteID {
            foundSite = true
            require.NotNil(t, site.Manager, "Manager should not be null")
            assert.Equal(t, managerID.String(), site.Manager.ID)
            assert.Equal(t, "测试管理员", site.Manager.Name)
            break
        }
    }
    assert.True(t, foundSite, "Created site should be in the list")
    
    // 清理
    db.Exec(`DELETE FROM sites WHERE tenant_id = ?`, tenantID)
    db.Exec(`DELETE FROM users WHERE tenant_id = ?`, tenantID)
}
```

## ✅ 验收标准

1. ✅ 显式指定 GORM 列名
2. ✅ 添加调试日志验证数据流
3. ✅ 检查 lookupOrSyncManager 调用
4. ✅ 创建单元测试覆盖场景：
   - 调用 API 创建网点
   - 调用 GET /api/sites/tree?root=
   - 验证网点对应项中提供 manager_name
5. ✅ 运行 `go build` 编译通过
6. ✅ 单元测试通过

## 📂 涉及文件

| 文件路径 | 操作 | 说明 |
|---------|------|------|
| `backend/models/models.go` | 修改 | 显式指定 manager_id 列名 |
| `backend/handlers/site.go` | 修改 | 添加调试日志 |
| `backend/handlers/site_test.go` | 新建/修改 | 添加单元测试 |

## 🔧 验证步骤

1. 修改代码并编译
2. 运行单元测试：`go test -v -run TestGetSiteTreeReturnsManager ./handlers/...`
3. 测试通过后再进行集成测试
4. 查看后端日志中的 ManagerID 值
5. 调用 GET /api/sites/tree
6. 验证响应中 manager 对象非 null

---
*Model: zhipuai/glm-5*